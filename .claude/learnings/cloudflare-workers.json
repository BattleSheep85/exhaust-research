{
  "domain": "cloudflare-workers",
  "description": "Cloudflare Workers runtime gotchas and platform quirks",
  "file_patterns": [
    "src/**/*.ts",
    "src/**/*.js"
  ],
  "entries": [
    {
      "id": "date-frozen-at-module-init",
      "bad_pattern": "Calling new Date() or Date.now() at module top-level to compute values like CURRENT_YEAR",
      "good_pattern": "Compute time-dependent values inside request handlers, not at module init",
      "regex": "^(const|let|var)\\s+\\w+\\s*=\\s*new Date\\(\\)",
      "description": "Cloudflare Workers freeze the clock at the Unix epoch (Jan 1 1970) during module initialization — real time is only available once a request handler runs. Any `new Date()` or `Date.now()` call at the top level of a module (e.g. `const CURRENT_YEAR = new Date().getUTCFullYear()`) will return 1970, not the actual current year. This silently corrupts downstream logic like search queries that interpolate the year, LLM system prompts that reference 'the current year', rate-limit windows, cache keys, etc. Always move time-dependent computation inside a function that runs per-request. This is part of CF Workers' security model to prevent timing side channels before the first I/O.",
      "context": "Cloudflare Workers, src/lib/*.ts, module-level constants",
      "discovered": "2026-04-11",
      "severity": "HIGH"
    },
    {
      "id": "waituntil-30s-hard-limit",
      "bad_pattern": "ctx.waitUntil(longRunningJob()) for work that can exceed 30 seconds",
      "good_pattern": "Either await the job inline in the fetch handler (15-min request-duration limit on paid plan) OR offload to Queues / Durable Objects / Workflows",
      "regex": "ctx\\.waitUntil\\(",
      "description": "ctx.waitUntil() gets ONLY 30 seconds of extension after the fetch handler returns — regardless of `limits.cpu_ms` in wrangler.jsonc. `cpu_ms` bounds the fetch handler's active CPU time, it does NOT extend the waitUntil tail. If the tail exceeds 30s, CF kills it silently with a warn-level log (`waitUntil() tasks did not complete within the allowed time after invocation end and have been cancelled`) — no exception is raised in user code, so any `try/catch` inside the waitUntil job never runs. This makes background jobs look like hangs: D1 rows inserted as 'processing' stay 'processing' forever because the outer catch that would mark 'failed' is inside the killed isolate. The 15-min duration limit on paid plans only applies to the fetch handler's own lifetime (i.e. while it is still awaiting), not the tail. To run long jobs: (a) await them before returning the response (user waits for long POST), (b) use a streaming Response body so the handler itself stays active while the client reads, or (c) enqueue to CF Queues / Durable Objects / Workflows.",
      "context": "Cloudflare Workers, long-running background work, src/pages/api.ts-style endpoints",
      "discovered": "2026-04-15",
      "severity": "CRITICAL"
    },
    {
      "id": "fetch-handler-cancelled-on-client-disconnect",
      "bad_pattern": "await longJob() inside a fetch handler for work the user may not wait for (tab close, network drop, curl --max-time shorter than job, upstream CF gateway 502)",
      "good_pattern": "Return a streaming Response (SSE / chunked body) so CF keeps the isolate alive as long as the client reads, OR enqueue to CF Queues / Durable Objects / Workflows for true fire-and-forget background work",
      "regex": null,
      "description": "Cloudflare cancels the fetch handler the MOMENT the client disconnects — verified in `wrangler tail --format json` as `outcome: 'canceled'` with a wallTime matching the client's abort time, not the job's expected duration. Any `await longOp()` inside the handler is killed mid-flight; D1 rows created before the await stay in 'processing' forever because the outer catch is in the killed isolate (same orphan pattern as waitUntil-30s-hard-limit). Triggers: curl --max-time shorter than job, browser tab close, mobile background switch, flaky network, or a transient 502 from an upstream CF gateway even when the client itself is still waiting. The 'switch from ctx.waitUntil to inline await' fix only works while the client stays connected for the full job duration — it doesn't address disconnects. Streaming Response bodies are the lightest idiomatic fix: the handler stays active while the client reads events, and a well-designed client can reconnect and resume. Real solutions for true background work: (a) CF Queues, (b) Durable Objects, (c) Workflows. Cleanup hygiene: add a cron that marks `status='processing' AND created_at < now - maxExpectedDuration` as failed, because orphaned rows are inevitable under this pattern.",
      "context": "Cloudflare Workers, long-running POST handlers, any code that writes a DB row before awaiting a multi-second operation",
      "discovered": "2026-04-15",
      "severity": "HIGH"
    }
  ]
}
